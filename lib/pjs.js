var pjs = {};

pjs.filter = function(lines, explicit) {
  return lines;
};

pjs.map = function(lines, explicit) {
  return lines;
};

pjs.reduce = function(lines, explicit) {
  return lines;
};

module.exports = pjs;
